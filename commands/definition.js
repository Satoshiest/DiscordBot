module.exports = {
    name: 'definition',
    description: 'Returns the definition of word from wikipedia.',
    async execute(client, message, cmd, args, Discord){
        message.channel.send('Sorry this command is not implemented yet...');
    }
}